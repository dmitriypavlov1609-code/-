# Core dependencies for PostgreSQL support
psycopg2-binary==2.9.9
pgvector==0.2.4

# Note: Bot primarily uses Python stdlib + Telegram HTTP API + LLM HTTP API
# PostgreSQL dependencies are optional - bot falls back to SQLite if not available
